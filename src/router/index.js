import { Course, Home } from '../pages';

const router = [
   { element: <Home />, path: '/' },
   { element: <Course />, path: '/' },
   { element: <Course />, path: '/' },
   { element: <Course />, path: '/course' },
   { element: <Course />, path: '/' },
   { element: <Course />, path: '/' },
];

export default router;
